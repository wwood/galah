channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # These depedencies are spelled out in the README.md too, so changes need to be synced manually.
  - dashing >= 0.4.0
  - fastani >= 1.3
  - skani >= 0.1.1
  - extern
